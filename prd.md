# 📄 [PRD] 바른 자세 코치 웹사이트 (Posture Coach Web)

---

## 1. 프로젝트 개요 (Overview)

* **프로젝트명**: 웹캠 및 아두이노 네오픽셀 연동 실시간 바른 자세 코치 웹사이트
* **목적**: 웹캠 영상을 이용하여 사용자의 자세(바른 자세 vs 거북목 자세)를 실시간으로 분석하고, 그 결과를 웹 인터페이스와 아두이노의 네오픽셀(WS2812B) LED 색상으로 직관적으로 알려주어 거북목을 예방하도록 돕습니다.
* **타겟 사용자**: PC/모니터 앞에 오래 앉아 있는 직장인, 학생 및 IoT/인공지능 초보 개발자
* **핵심 가치**: 초보자도 별도의 아두이노 코드 수정 없이 범용 펌웨어(@Webserialtest)를 그대로 사용하여 웹과 시리얼 통신 기반 IoT 디바이스를 완성할 수 있도록 함.

---

## 2. 기술 스택 및 개발 환경 (Tech Stack & Environment)

| 구분 | 사용 기술 / 하드웨어 | 비고 |
| :--- | :--- | :--- |
| **웹 프론트엔드** | HTML5, CSS3, JavaScript (Vanilla JS) | 별도의 무거운 프레임워크 없이 웹 표준 기술 사용 |
| **비전 / AI** | MediaPipe Pose | 웹 브라우저 상에서 실시간 랜드마크 추적 |
| **브라우저-아두이노 통신** | Web Serial API | 크롬/에지 등 Chromium 기반 브라우저 지원 (115200 bps) |
| **마이크로컨트롤러** | 아두이노 우노(Arduino UNO) 등 | 범용 펌웨어 (`Webserialtest.ino`) 업로드 완료 상태 |
| **출력 디바이스** | Adafruit NeoPixel (4개 LED) | D7 핀 연결 (펌웨어 정의 `PIN_NEOPIXEL 7`) |

---

## 3. 핵심 사용자 흐름 (User Flow)

1. **웹사이트 접속**: 웹페이지에 접속하여 시스템 개요 및 연결 상태 확인.
2. **아두이노 연결**: `[아두이노 연결]` 버튼 클릭 후 포트 선택 (Web Serial 연결 확립).
3. **웹캠 시작**: `[카메라 시작]` 버튼 클릭하여 웹캠 권한 허용 및 실시간 영상 표시.
4. **자세 분석 중**: 카메라 시야 내에 사용자가 들어오면 MediaPipe 랜드마크 감지 시작 (네오픽셀: **노란색**).
5. **자세 상태 피드백**:
   * **바른 자세 감지**: 웹 화면 "바른 자세" 표시, 네오픽셀 **초록색** 점등.
   * **거북목 자세 감지**: 웹 화면 "거북목 주의" 표시, 네오픽셀 **빨간색** 점등.
   * **3초 이상 거북목 유지**: 웹 화면 경고 팝업 및 시각적 애니메이션 발동.
6. **종료 / 해제**: `[카메라 종료]` 또는 `[연결 해제]` 클릭 시 네오픽셀 소등 (OFF).

---

## 4. 상세 기능 요구사항 (Functional Requirements)

### 4.1 웹캠 및 영상 스트림 제어
* **웹캠 촬영 시작/종료**: 사용자가 버튼 클릭으로 웹캠을 켜거나 끌 수 있어야 함.
* **실시간 화면 표시**: 웹캠 영상 스트림을 `<video>` 태그 및 `<canvas>` 태그를 이용하여 실시간으로 렌더링.
* **미디어 권한 처리**: 브라우저의 카메라 권한 거부 시 안내 메시지 출력.

### 4.2 아두이노 연결 및 Web Serial 통신 관리
* **웹 시리얼 연결 및 해제**: `navigator.serial.requestPort()`를 통한 통신 포트 선택 및 연결 종료 기능 제공.
* **연결 상태 표시 UI**: "연결 안 됨", "연결됨 (COMx)", "통신 오류" 등 현재 아두이노 연결 상태를 실시간 배지로 표시.
* **Baud Rate**: **115200 bps** 고정 사용.

### 4.3 MediaPipe Pose 랜드마크 감지 및 자세 분석
* **실시간 포즈 탐지**: 웹캠 영상 프레임별로 사람의 신체 랜드마크 좌표 추적.
* **옆모습(Profile) 분석 기준**:
  * 사용자의 **귀(Ear)** 랜드마크 (`Landmark 7` 또는 `8`)
  * 사용자의 **어깨(Shoulder)** 랜드마크 (`Landmark 11` 또는 `12`)
* **자세 판단 로직**:
  1. 귀와 어깨를 잇는 선의 각도($\theta$) 측정.
  2. 어깨 대비 귀가 앞으로 돌출된 X축 이격 거리($\Delta X$) 측정.
  3. 측정값이 사용자가 설정한 **임계값(Threshold)**을 초과하면 "거북목 자세"로 판정.

### 4.4 사용자 임계값 조절 UI
* **민감도 조절 슬라이더**: 사용자의 체형이나 카메라 각도에 맞게 거북목 판단 각도/거리 기준을 조절할 수 있는 슬라이더 UI 제공.
* **기본값 제공**: 초보자를 위해 권장 기본값(예: 15도)이 사전에 입력되어 있도록 구현.

### 4.5 자세 상태 표출 및 경고 알림
* **웹 화면 상태 표시**:
  * `자세 분석 중` (노란색 뱃지)
  * `바른 자세` (초록색 뱃지)
  * `거북목 자세` (빨간색 뱃지)
  * `자세 미감지 / 웹캠 이탈` (회색 뱃지)
* **3초 지속 경고 팝업**: 거북목 자세가 **3초 이상 지속**되는 경우 웹 화면 전체에 빨간 붉은색 테두리 또는 경고 팝업 창을 띄워 시각적으로 자극.

### 4.6 네오픽셀 LED 제어 및 통신 최적화
* **범용 펌웨어 Protocol 준수**:
  * 명령 형식: `SET,NEO,255,R,G,B\n`
  * 응답 형식: `OK:NEO\n`
* **LED 색상 규약**:
  * **자세 분석 중**: 노란색 `SET,NEO,255,255,255,0\n`
  * **바른 자세**: 초록색 `SET,NEO,255,0,255,0\n`
  * **거북목 자세**: 빨간색 `SET,NEO,255,255,0,0\n`
  * **연결 해제 / 종료**: LED 끄기 `SET,NEO,255,0,0,0\n`
* **통신 최적화 (Traffic Control)**:
  * 프레임마다 매번 시리얼 명령을 보내지 않고, **자세 상태가 변경되는 순간에만 1회 전송**.
  * 시리얼 통신 과부하 및 아두이노 버퍼 랙 방지.

### 4.7 오류 및 에러 처리 (Error Handling)
* **자세 감지 실패**: 사람이 카메라 프레임 밖으로 벗어나거나 랜드마크 감지가 안 될 경우 화면에 "사용자가 감지되지 않습니다" 안내.
* **시리얼 전송 오류**: 아두이노 케이블이 뽑히거나 전송 실패 시, 자동으로 상태를 "연결 끊김"으로 변경하고 사용자에게 재연결 안내 모달 표시.

---

## 5. 아두이노 펌웨어 규약 사양 (`Webserialtest.ino`)

> [!IMPORTANT]
> 본 프로젝트는 기존 아두이노 펌웨어(`Webserialtest.ino`)의 코드를 수정하거나 명령어를 추가하지 않고 **100% 그대로 활용**합니다.

```
[웹 브라우저 (JavaScript)] ─── Web Serial (115200bps) ───> [아두이노 (Webserialtest.ino)]
```

* **네오픽셀 제어 명령어 구조**:
  * `SET,NEO,인덱스,R,G,B\n`
  * **255 번 인덱스**: 4개 LED 전체 제어 규약 (`val1 == 255`)
* **예시 명령어**:
  * `SET,NEO,255,0,255,0\n` (초록색 - 바른 자세)
  * `SET,NEO,255,255,0,0\n` (빨간색 - 거북목 자세)
  * `SET,NEO,255,255,255,0\n` (노란색 - 분석 중)
  * `SET,NEO,255,0,0,0\n` (끄기)

---

## 6. 사용자 인터페이스 (UI/UX) 화면 구성안

```
+-----------------------------------------------------------------------+
|  🧩 바른 자세 코치 (Posture Coach)                   [🔌 아두이노 연결]  |
+-----------------------------------+-----------------------------------+
|                                   |  📊 현재 상태                     |
|  [ 실시간 웹캠 / MediaPipe Canvas ]  |  +------------------------------+ |
|                                   |  |  상태: 🟢 바른 자세          | |
|                                   |  +------------------------------+ |
|   (사용자 귀-어깨 랜드마크 표시)   |                                   |
|                                   |  ⚙️ 민감도 (거북목 임계 각도)       |
|                                   |  [====|-------------] 15°         |
|                                   |                                   |
|  [📷 카메라 시작] [🛑 카메라 종료]   |  💡 아두이노 통신 상태: OK          |
+-----------------------------------+-----------------------------------+
|  ⚠️ [알림] 거북목 자세가 3초 이상 유지되었습니다! 자세를 바로잡으세요.  |
+-----------------------------------------------------------------------+
```

---

## 7. 비기능 요구사항 (Non-Functional Requirements)

1. **초보자 편의성**:
   * 아두이노 C++ 코드 작성 없이 웹 UI 조작만으로 동작을 완벽히 이해하고 테스트할 수 있어야 함.
2. **반응성 및 성능**:
   * MediaPipe Pose 탐지 속도: 최소 15~30 FPS 유지.
   * 시리얼 명령 전송 지연 시간: 100ms 이내.
3. **안정성**:
   * 웹 시리얼 연결이 끊어져도 웹 브라우저가 다운되지 않고 오류 메시지 수신 후 안전하게 재연결을 유도함.

---

## 8. 단계별 개발 순서 및 상세 구현 기능 (Development Sequence)

본 프로젝트는 아래 4단계 흐름에 따라 단계별로 기능을 검증하며 개발을 진행합니다.

```
[1단계: 메인 화면 제작] ➡️ [2단계: 웹캠 & MediaPipe 감지] ➡️ [3단계: 자세 분석 & 상태 판정] ➡️ [4단계: 아두이노 & 네오픽셀 제어]
```

---

### 8.1 1단계: 메인 화면 제작 (Main UI & Layout)
* **목적**: 웹 애플리케이션의 기본 사용자 인터페이스(UI) 구조 및 스타일을 완성합니다.
* **구현 기능 목록**:
  1. **대시보드 레이아웃 구축**: 웹캠/캔버스 표시 영역, 제어 버튼 영역, 상태 표시 카드, 설정 영역 분리.
  2. **상태 표시 뱃지 UI**: 현재 자세 상태(분석 중, 바른 자세, 거북목 주의, 미감지)별 색상 및 디자인 구현.
  3. **컨트롤 요소 배치**: `[카메라 시작/종료]` 버튼, `[아두이노 연결/해제]` 버튼, 민감도(임계값) 조절 슬라이더 배치.
  4. **시각적 알림 영역 레아아웃**: 3초 이상 거북목 유지 시 표시될 화면 경고 팝업 및 빨간 붉은색 테두리 오버레이 레이아웃 구성.

---

### 8.2 2단계: 웹캠 실행 및 MediaPipe 자세 감지 (Webcam & Pose Detection)
* **목적**: 웹 브라우저에서 웹캠 영상을 획득하고 MediaPipe Pose를 연동하여 실시간 신체 랜드마크를 추출합니다.
* **구현 기능 목록**:
  1. **웹캠 스트림 연동**: `navigator.mediaDevices.getUserMedia()`를 이용한 실시간 비디오 획득 및 `<video>` 태그 표시.
  2. **카메라 제어 로직**: 시작/종료 버튼 클릭 시 웹캠 스트림 켜기/끄기 및 메모리 해제 처리.
  3. **MediaPipe Pose 로드**: 브라우저 환경에서 MediaPipe Pose 라이브러리 및 솔루션 초기화.
  4. **실시간 랜드마크 시각화**: 카메라 프레임마다 신체 포인트 및 스켈레톤 연결선을 `<canvas>` 상에 렌더링.

---

### 8.3 3단계: 자세 분석 및 상태 판정 (Posture Analysis & Evaluation)
* **목적**: MediaPipe에서 추적한 랜드마크 좌표를 분석하여 거북목 여부를 판정하고 상태를 웹 화면에 업데이트합니다.
* **구현 기능 목록**:
  1. **옆모습 랜드마크 추출**: 사용자의 **귀(Ear: 7/8번)** 및 **어깨(Shoulder: 11/12번)** 좌표값 수집.
  2. **자세 측정 알고리즘 구현**:
     * 귀-어깨 잇는 선의 각도($\theta$) 및 X축 전방 이격 거리($\Delta X$) 계산 logic 작성.
  3. **임계값(Threshold) 연동**: 사용자가 민감도 슬라이더를 조절함에 따라 판정 기준값을 실시간 반영.
  4. **자세 상태 판정 및 화면 업데이트**:
     * 측정값과 임계값 비교를 통해 `바른 자세`, `거북목 자세`, `자세 분석 중` 상태를 즉시 뱃지에 반영.
  5. **3초 알림 타이머 로직**: 거북목 상태가 3초 연속 유지될 경우 경고 팝업 및 애니메이션 발동.

---

### 8.4 4단계: 아두이노 연결 및 네오픽셀 제어 (Arduino & NeoPixel Control)
* **목적**: Web Serial API를 이용하여 아두이노와 통신하고, 자세 판정 결과에 따라 네오픽셀 LED 색상을 제어합니다.
* **구현 기능 목록**:
  1. **Web Serial 통신 모듈 작성**: `navigator.serial.requestPort()` 기반 아두이노 연결/해제 및 Baud Rate 115200bps 설정.
  2. **연결 상태 모니터링**: 아두이노 통신 상태(연결됨/해제됨/오류)를 웹 화면 배지에 실시간 표출.
  3. **호환 시리얼 명령어 송신**: `@Webserialtest` 펌웨어 규약(`SET,NEO,255,R,G,B\n`)에 맞춘 명령 생성 모듈 개발.
  4. **네오픽셀 LED 색상 제어 연동**:
     * 자세 분석 중: 노란색 `SET,NEO,255,255,255,0\n`
     * 바른 자세: 초록색 `SET,NEO,255,0,255,0\n`
     * 거북목 자세: 빨간색 `SET,NEO,255,255,0,0\n`
     * 연결 종료: OFF `SET,NEO,255,0,0,0\n`
  5. **시리얼 전송 최적화 (Traffic Control)**: 자세 상태가 변경되는 시점에만 시리얼 명령을 1회 전송하도록 이벤트 트리거 처리.

---
*작성일: 2026년 7월 31일*  
*작성자: 프로덕트 매니저 (IoT Service Design)*

